{% extends 'SytemSGBundle:Default:baseSolicitacoes.html.twig' %}
{% if is_granted('ROLE_USER') %}
{% block title %}Usuário Comum{% endblock %}
{# block col %}
<div class="collapse navbar-collapse navbar-ex1-collapse">

    <ul class="nav navbar-nav side-nav">
      
        <li><a href="{{ path('newsolicitacao') }}">  <strong>Solicitar  de Mac </strong></a></li>    
        <li> <a href="{{ path('search_user') }}" style="font-weight:bold;"> Gerenciar Mac</a> </li>
        <li> <a href="{{ path('search_usernovanotificacao') }}" style="font-weight:bold;"> Solicitações em Andamento</a> </li>
        <li> <a href="{{ path('tutorial') }}" style="font-weight:bold;"> Tutorial</a> </li>      
    </ul>
</div>
{% endblock col #}
{% block imagem %}



 
{% endblock imagem %}


{% block body %}
<br>
<ol class="breadcrumb">
                                    <li>
                                     <a href="{{ path('useradmin') }}">Home SysNTI - NetManager</a>
                                        
                                    </li>
                                  
     </ol>

 <div class="form-group">

     {% for flashMenssage in app.session.flashbag.get('message') %}
      <div class="alert alert-info" style="text-align: center; font-size: 17px;"> 
           <button  type="button" class="close" data-dismiss="alert">x</button>
            <strong>{{ flashMenssage }}</strong>
      </div>
     {% endfor %} 
  </div>
                  
  <div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">  <i class="glyphicon glyphicon-user"></i> Usuario: <strong>{{ user.nome }}</strong>. Seja Bem Vindo.  </h3>
  </div>

  <div class="panel-body">
   <p style="text-align:center;font-size:16px"><center> <img  width="500" class="img-responsive" src="{{ asset('bundles/sytemsg/images/nt2.bmp') }}"></center></p>
   
   <div class="table-responsive">
    <!--<table class="records_list">-->
    <table class="table ">
          <tr>
            <th> Nome:</th>
            <td>{{ user.nome }}</td>
          </tr>
       
          <tr>
            <th> Email/ Login:</th>
            <td>{{ user.email }}  </td>
          </tr>
        
          <tr>
            <th> Tipo:</th>
            <td> {% for entity in user.roles %}
                   {{ entity }}
                 {% endfor %}
            </td>
          </tr>
         
    </table></center>
  </div>
  </div>
</div>                
    





{% endblock %}
{% endif %} 